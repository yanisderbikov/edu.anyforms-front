// Runtime-конфиг. На стендах этот файл перегенерируется при старте
// (scripts/inject-env.mjs) из переменных окружения — пересборка не нужна.
// Локально пустой: адрес бэка берётся из .env.development.
window.__ENV__ = {};
